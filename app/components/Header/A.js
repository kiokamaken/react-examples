import styled from 'styled-components';
import NormalA from 'components/A';

const A = styled(NormalA)`
  padding: 2em 0;
  display: block;
  height: 250px;
  position: relative;
  overflow: hidden;
`;

export default A;
